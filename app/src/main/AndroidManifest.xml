<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />

    <application
        android:name=".Constant.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_no_bg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_no_bg"
        android:supportsRtl="true"
        android:theme="@style/Theme.MilkFlow"
        tools:targetApi="31">
        <activity
            android:name=".Activity.ProfileUpdateActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:label="appwrite_callback">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="success"
                    android:scheme="appwrite-callback-6835f6ca000039dac8be" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.MainActivity"
            android:exported="false" /> <!-- app write -->
        <!-- Add this inside the `<application>` tag, along side the existing `<activity>` tags -->
        <!--        <activity-->
        <!--            android:name="io.appwrite.views.CallbackActivity"-->
        <!--            android:exported="true">-->
        <!--            <intent-filter android:label="android_web_auth">-->
        <!--                <action android:name="android.intent.action.VIEW" />-->

        <!--                <category android:name="android.intent.category.DEFAULT" />-->
        <!--                <category android:name="android.intent.category.BROWSABLE" />-->

        <!--                <data android:scheme="appwrite-callback-6835f6ca000039dac8be" />-->
        <!--            </intent-filter>-->
        <!--        </activity>-->

        <service
            android:name=".Services.FirebaseCloudMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>